<template name="product-item">
  <view class="product-item flex flex-between">
    <view><image src="{{item.img}}"></image></view>
    <view class="flex flex-between">
      <view class="flex flex-column">
        <view>{{item.name}}</view>
        <view>{{item.price}}</view>
      </view>
      <view class="">
        icon
      </view>
    </view>
  </view>
</template>